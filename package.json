{
  "name": "expo-master-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm start"
  }
}
